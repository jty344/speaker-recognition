[project]
name = "voiceprint-demo"
version = "0.1.0"
description = "CPU-only open-set speaker identification demo"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = "-q"

