# 数据目录

把个人音频放在 `data/private/` 下。该目录默认不纳入版本控制。

支持的文件后缀为 `.mp3`、`.wav`、`.flac` 和 `.aac`，大小写不敏感。`.aac` 必须是裸 ADTS AAC；`.m4a` 等 MP4 容器暂不支持。

建议每名说话人准备：

- `enroll/`：3～5 段独立录音，每段 3～10 秒；
- `dev/`：用于选择识别阈值；
- `test/`：只用于最终检查。

每个文件应只有一名主要说话人。
